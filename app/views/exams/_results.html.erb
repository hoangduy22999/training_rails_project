<div class="card-header">
  <h3 class="card-title">Exam</h3>
</div>
<!-- /.card-header -->
<div class="card-body">
  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>Subject</th>
        <th>Questions</th>
        <th>Time</th>
        <th>Result</th>
        <th>Submitted</th>
      </tr>
    </thead>
    <tbody>
      <% @search_result_exams.each.with_index(1) do |exam, index| %>
        <tr>
          <td><%= index %></td>
          <td><%= link_to exam.name, exam_detail_path(exam) %></td>
          <td><%= exam.get_subject_name.capitalize %></td>
          <td><%= exam.questions.count %></td>
          <td><%= "#{exam.time/60}m" %></td>
          <td><%= exam.result_average %></td>
          <td><%= exam.get_results %></td>
        </tr>
      <% end %>
      <%= paginate @search_result_exams %>
    </tbody>
  </table>
</div>